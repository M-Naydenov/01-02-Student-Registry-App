version: '3'
services:
  app:
    image: gracerx/students:1
    ports:
      - "8081:8081" # Map port 8081 on the host to port 8081 
      # in the container since this is the port of JENKINS
      
    environment:
      - NODE_ENV=production
